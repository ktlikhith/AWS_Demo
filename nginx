# Create directory
sudo mkdir -p /usr/share/nginx/html

# Copy build files
sudo cp -r build/* /usr/share/nginx/html/

# Set permissions
sudo chmod -R 755 /usr/share/nginx/html
Step 5 — Configure Nginx
bashsudo vi /etc/nginx/conf.d/bookstore.conf

server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Serve React app
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy /books to Internal ALB
    location /books {
        proxy_pass http://internal-AppLB-780513591.ap-south-1.elb.amazonaws.com/books;
        proxy_http_version 1.1;
        proxy_set_header Host internal-AppLB-780513591.ap-south-1.elb.amazonaws.com;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

Step 6 — Remove Default Nginx Config
bash# Disable default config
sudo mv /etc/nginx/conf.d/default.conf /etc/nginx/conf.d/default.conf.bak

Step 7 — Test & Restart Nginx
bash# Test config syntax
sudo nginx -t

# If ok, restart
sudo systemctl restart nginx

npm run build
sudo cp -r build/* /usr/share/nginx/html/


PM2 Complete Commands
Step 1 — Install PM2
bashnpm install -g pm2
Step 2 — Start Your App
bash# Start app
pm2 start app.js --name "bookstore-api"

# Or if using npm start
pm2 start npm --name "bookstore-api" -- start
Step 3 — Save PM2 Process List
bash# Save current running processes
pm2 save
Step 4 — Enable PM2 on System Reboot
bash# Generate startup script
pm2 startup

# It will give you a command to run like:
sudo env PATH=$PATH:/usr/bin /usr/lib/node_modules/pm2/bin/pm2 startup systemd -u ec2-user --hp /home/ec2-user

# Copy and run that exact command!
Step 5 — Verify
bash# Reboot and check
sudo reboot

# After reboot SSH in and check
pm2 list


npm install